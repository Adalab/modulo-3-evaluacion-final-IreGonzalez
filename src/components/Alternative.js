import '../styles/Variables.scss';
import '../styles/Alternative.scss';

const Alternative = () => {
    return (
        <section className="alternative">
            <h2 className="alternative__insult">¡¡Eh!!</h2>
            <p className="alternative__text">¡¡Mete unos datos que sean correctos!!</p>
        </section>
    );
};

export default Alternative;