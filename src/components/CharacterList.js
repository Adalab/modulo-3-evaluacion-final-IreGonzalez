import CharacterCard from './CharacterCard'

const CharacterList = () => {
    return (
        <ul className="list">
            <CharacterCard />
        </ul>
    );
};

export default CharacterList;